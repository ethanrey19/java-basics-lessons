package main.Misc.StringMethods;

public class StringMethods {

    public static void main(String[] args) {
        // String = a reference data type that can store one or more characters

        String name = "Ethan";

        // boolean result =  name.equalsIgnoreCase("Ethan");  / equal to the value ignoring case
        // int result = name.length();          / number of characters
        // char result = name.charAt(0);        / find a character from the index
        // int result = name.indexOf("E");      / index of a character
        // boolean result = name.isEmpty();     / check to see if string is empty
        // String result = name.toUpperCase();  / makes the value all uppercase
        // String result = name.toLowerCase();  / makes the value all lowercase
        // String result = name.trim();         / gets rid of all empty space
        // String result = name.replace(oldChar,newChar); / replaces one character wth another

        // System.out.println(result);
    }
}
